;;; Preparations

;; Turn off paredit-mode (until I take time to learn it).
;(add-hook 'emacs-lisp-mode-hook (lambda () (paredit-mode -1)))
;(add-hook 'clojure-mode-hook (lambda () (paredit-mode -1)))
